<% content_for :title do %>FTC League Meets Result Search<%end %>
<p id="notice"><%= notice %></p>

<h1>League Meets</h1>


<p>Note: if you have result files, send them to <a href="mailto:help@ftcroot.com">help@ftcroot.com</a></p>
<p>A lot of events have been added, but it is no means all of it. </p>
<p> Thank you to everybody who has sent in meet results! </p>

<br/>

<%= form_tag(league_meets_path(""), method: "get",:class => "pure-form") do %>
  <p>
    <%= label_tag(:search, "Search Meet name, location or date:") %>
    <%= text_field_tag(:search,params[:search]) %>
    <%= submit_tag("Search", :class=>"pure-button pure-button-primary") %>
  </p>
<% end %>
<table class="pure-table">
    <thead>
        <tr>
            <th>Name</th>
            <th>Approx. Date</th>
            <th>Location</th>

            <!-- <th colspan="3"></th> -->
        </tr>
    </thead>

    <tbody>
        <% @league_meets.each do |league_meet| %>
        <tr>
            <td><%= link_to league_meet.name, league_meet %></td>
            <td><%= league_meet.date %></td>
            <td><%= link_to league_meet.location, league_meet.region %></td>
        </tr>
        <% end %>

        <%= will_paginate @league_meets %>
    </tbody>
</table>

<br>

<%#link_to 'New League Meet', new_league_meet_path %>
